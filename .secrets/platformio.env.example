# Copy to .secrets/platformio.env and replace the example values.
# If a value contains spaces or shell-special characters, wrap it in double quotes.

WIFI_SSID="YOUR_WIFI_NAME"
WIFI_PASSWORD="YOUR_WIFI_PASSWORD"
